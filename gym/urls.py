from django.urls import path
from .views import *
urlpatterns = [
    path('', gym_index, name='home'),
    path('index/', HomeGym.as_view(), name='index'),
    # path('muscles/', gym_muscles, name='muscles'),
    path('muscles/', MuscleGroupView.as_view(), name='muscles'),
    # path('exercises/', gym_exercises, name='exercises'),
    # path('exercise/difficulty/<int:difficulty>', gym_exercise_by_diff, name='exercise_by_diff'),
    path('exercises/', SExerciseView.as_view(), name='exercises'),
    path('exercises/difficulty/<int:difficulty>', SExerciseView.as_view(), name='exercise_by_diff'),
    # path('exercise/<int:exercise_id>', gym_exercise, name='exercise'),
    path('exercise/<int:pk>', SExerciseDetail.as_view(), name='exercise'),
    # path('equipments/', gym_equipments, name='equipments'),
    path('equipments/', SEquipmentView.as_view(), name='equipments'),
    path('equipments/<int:id_type>', SEquipmentView.as_view(), name='equipments'),
    path('equipment/<int:pk>', SEquipmentDetail.as_view(), name='equipment'),
    path('client/', gym_client, name='client'),
    path('almanac/', gym_almanac, name='almanac'),
    # path('muscle-group/<int:group_id>', get_muscle_group, name='muscle_group'),
    # path('muscle-group/<int:group_id>', MusclesByGroup.as_view(), name='muscle_group'),
    path('muscle-group/<int:pk>', MuscleGroupDetail.as_view(), name='muscle_group'),
    path('strainings/', STrainingView.as_view(), name='strainings'),
    path('straining/<int:pk>', STrainingDetail.as_view(), name='straining'),
    path('muscle/<int:muscle_id>', get_muscle, name='muscle'),
    # path('add-muscle/', add_muscle, name='add_muscle'),
    path('add-muscle/', CreateMuscle.as_view(), name='add_muscle'),
    path('add-sequipment/', CreateSEquipment.as_view(), name='add_sequipment'),
    path('add-sexercise/', CreateSExercise.as_view(), name='add_sexercise'),
    path('update-traininground/<int:pk>', UpdateTrainingRound.as_view(), name='update_traininground'),
    path('add-traininground/<int:texercise_id>', CreateTrainingRound.as_view(), name='add_traininground'),
    path('add-trainingexercise/<int:straining_id>', CreateTrainingExercise.as_view(), name='add_trainingexercise'),
    path('add-equipmentusing/<int:s_exercise_id>', CreateEquipmentUsing.as_view(), name='add_equipmentusing'),
    path('update-equipmentusing/<int:pk>', UpdateEquipmentUsing.as_view(), name='update_equipmentusing'),
    path('add-muscleusing/<int:s_exercise_id>', CreateMuscleUsing.as_view(), name='add_muscleusing'),
    path('update-muscleusing/<int:pk>', UpdateMuscleUsing.as_view(), name='update_muscleusing'),
    path('add-straining/', CreateSTraining.as_view(), name='add_straining'),
    path('update-straining/<int:pk>', UpdateSTraining.as_view(), name='update_straining'),
]
